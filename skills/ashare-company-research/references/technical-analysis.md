# 技术面证据边界 —— 已迁移至共享层

本文件的内容已上移为**全库共享**：

> **[`../../_shared/technical-analysis.md`](../../_shared/technical-analysis.md)**

原因：`ashare-news-investment-targets` 的阶段C 也需要同一套技术面口径。此前两个技能各写一份，并且结论相反（本技能禁止估算价位，新闻技能要求给出止损止盈）。2026-09-10 审计后收敛为单一真源。

**不要在本文件内重新写技术面规则。** 修改技术面口径请改共享文件，并跑 `make test`。

共享文件相对本技能原有版本新增的内容：

- **硬禁令清单**：明确列出数据门未通过时禁止输出的全部项（股价、均线、MACD、成交量、支撑压力、中枢、买卖点、止损价、止盈价、买入观察价、目标价位、建仓价），并禁止用「区间估算」变通。
- **缺数据时的替代写法**：应该怎么写，而不是简单拒答。
- **滚动均值窗口纪律**：取 5/20 日均值时的数据长度要求（`lmt` 至少 25），避免 20 日均值被静默算成更短窗口。
- **买卖点的定位说明**：明确「买卖点」是结构分类，不是操作指令。
- **多标的场景**：新闻映射类任务一次覆盖多个标的时的处理规则。
